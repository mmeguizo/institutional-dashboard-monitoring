export interface IcampusDropdown {
    name: string;
    code: string;
}

export interface IdepartmentDashboardDropdown {
    name: string;
    code: string;
    id: string;
}

export interface genericDropdown {
    name: string;
    code: string;
    id: string;
}
